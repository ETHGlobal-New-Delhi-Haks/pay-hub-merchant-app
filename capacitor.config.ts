import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pay_hab.merchant.app',
  appName: 'pay-hub-merchant',
  webDir: 'dist'
};

export default config;
